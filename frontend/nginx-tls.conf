# =====================================================================
#  Cấu hình nginx CÓ HTTPS (TLS) cho production.
#  Dùng thay cho nginx.conf khi deploy thật. Cần:
#    - Chứng chỉ tại /etc/nginx/certs/fullchain.pem và /etc/nginx/certs/privkey.pem
#      (Let's Encrypt thật, hoặc tự ký để test: scripts/gen-self-signed-cert.sh)
#    - Mount thư mục cert + mở cổng 443 (xem DOCKER.md mục TLS).
# =====================================================================

# HTTP :80 -> chuyển hướng toàn bộ sang HTTPS.
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    # Bảo mật cơ bản
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;   # backend nhận đúng https để dựng URL/đối soát
        proxy_read_timeout 120s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
